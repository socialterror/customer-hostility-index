name: H06 Batch07A Industrials Blind Evidence
on:
  push:
    branches: [h06-manifest-verification-temp]
    paths:
      - '.github/workflows/h06-batch07a-industrials.yml'
permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: |
          cp h06_temp/build_t0_packets.py /tmp/b07a.py
          sed -i 's/MIN_ORDER=10/MIN_ORDER=127/' /tmp/b07a.py
          sed -i 's/MAX_ORDER=35/MAX_ORDER=141/' /tmp/b07a.py
          sed -i 's/packet_summary_batch02.csv/packet_summary_batch07a.csv/' /tmp/b07a.py
          sed -i "s#ROOT=pathlib.Path(__file__).resolve().parent#ROOT=pathlib.Path('$GITHUB_WORKSPACE/h06_temp')#" /tmp/b07a.py
          python /tmp/b07a.py
      - run: |
          git config user.name "h06-t0-bot"
          git config user.email "actions@users.noreply.github.com"
          git add h06_temp/t0_packets/1??_*.md h06_temp/t0_packets/packet_summary_batch07a.csv
          git diff --cached --quiet || git commit -m "Generate H06 Industrials blind evidence 127-141"
          git pull --rebase origin h06-manifest-verification-temp
          git push
# retrigger
