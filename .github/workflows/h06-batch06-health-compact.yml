name: H06 Batch06 Health Care Compact Review
on:
  push:
    branches: [h06-manifest-verification-temp]
    paths:
      - 'h06_temp/make_batch06_health_compact.py'
      - '.github/workflows/h06-batch06-health-compact.yml'
permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: python h06_temp/make_batch06_health_compact.py
      - run: |
          git config user.name "h06-t0-bot"
          git config user.email "actions@users.noreply.github.com"
          git add h06_temp/t0_review/batch06_health_care_compact.tsv
          git diff --cached --quiet || git commit -m "Build compact H06 Health Care review matrix"
          git pull --rebase origin h06-manifest-verification-temp
          git push
